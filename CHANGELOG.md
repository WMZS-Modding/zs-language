# 🛠 What's new in The Indent Update
- 🔍 New `indentationProvider.ts` with only checker, not for closing blocks
- ✏ Register block starters in `language-configuration.json`
- 🔧 Fix coloring `then` as `zs.red` to `keyword.control` and add `new` keyword in `zs.tmLanguage.json`