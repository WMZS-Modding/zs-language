# 🛠 What's changed
- 🔧 Minor update for `zs.tmLanguage.json`:
    + 🔨 Let `for`, `from`, `import`, `as`, `do` as keywords, not the complex patterns
    + ➕ Add `give`, `back` and `nothing` as the solution for `return`
    + ✏ Add flag `(?i)` for prepositions and preprocessors
- 🗂 Add keywords to exception list in `diagnosticProvider.ts`