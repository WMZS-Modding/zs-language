# 🛠 What's changed in 1.4.4:
- ➕ Added new Guillemet quotes (‹› and «»)
- ➕ Added alternative boolean keywords: `correct` and `wrong`
- 🖌 Color/Recolor:
  + `true`: variable.language.zs -> zs.green.zs
  + `false`: variable.language.zs -> zs.red.zs
  + `correct`: zs.green.zs
  + `wrong`: zs.red.zs
- 🔧 Added `false` and `wrong` to suffixExceptions list to avoid false positives